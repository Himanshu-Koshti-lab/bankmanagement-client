import { CreateAccountResponse } from './create-account-response';

describe('CreateAccountResponse', () => {
  it('should create an instance', () => {
    expect(new CreateAccountResponse()).toBeTruthy();
  });
});
