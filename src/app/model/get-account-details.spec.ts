import { GetAccountDetails } from './get-account-details';

describe('GetAccountDetails', () => {
  it('should create an instance', () => {
    expect(new GetAccountDetails()).toBeTruthy();
  });
});
