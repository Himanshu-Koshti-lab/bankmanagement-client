import { AccountCreationApproveRejectRequest } from './account-creation-approve-reject-request';

describe('AccountCreationApproveRejectRequest', () => {
  it('should create an instance', () => {
    expect(new AccountCreationApproveRejectRequest()).toBeTruthy();
  });
});
